import {
    runJSX
} from '../helpers/jsx.js'

const promisify = require('util').promisify

const {
    rename
} = require('fs-extra')
const path = require('path')


export const renameLink = () => (state, actions) => {


}